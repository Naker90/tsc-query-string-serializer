name: tests

on: [push]

jobs:
  run-tests:
    name: Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
      - uses: actions/checkout@v1
        
      - name: Install NPM depencies
        run: npm i

      - name: Run tests
        run: npm run test
